{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: test-specific d5p1",
            "request": "launch",
            "command": "npm run test-specific src/day6/part2/*.spec.ts",
            "cwd": "${workspaceFolder}"
        }
    ]
}